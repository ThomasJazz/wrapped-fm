# Tomcat webserver port
server.port=8081

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.hibernate.show-sql=true

### Datasource config
# See reasons for usage here: https://dzone.com/articles/bounty-spring-boot-and-postgresql-database
spring.datasource.initialization-mode=always
spring.datasource.initialize=true
spring.datasource.continue-on-error=true
spring.datasource.schema=classpath:/schema.sql

# Variables for connecting to our datasource (local postgres DB)
# spring.datasource.url=jdbc:postgresql://localhost:5432/wrapped_fm
# spring.datasource.username=postgres
# spring.datasource.password=postgres

# Variables for connecting to the dev digitical ocean postgresDB
spring.datasource.url=jdbc:postgresql://dev-sfo3-wfm-postgres-do-user-12281001-0.b.db.ondigitalocean.com:25060/wfm-dev
spring.datasource.username=${env.PG_USERNAME}
spring.datasource.password=${env.PG_PASSWORD}


username = ${env.username}
